/**
 * Propuesta técnica y económica — FCAB (Ferrocarril de Antofagasta a Bolivia).
 * Ruta pública: /pp-fcab (sin autenticación).
 * Renderiza el mismo template A4 paginado que /pp-ferronor con el cliente FCAB.
 */
import { PpFcab } from './PpFerronor';

export default PpFcab;
