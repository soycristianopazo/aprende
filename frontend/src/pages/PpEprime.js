/**
 * Propuesta técnica y económica — EPrime.
 * Ruta pública: /pp-eprime (sin autenticación).
 * Renderiza el template A4 paginado con el cliente EPrime.
 */
import { PpEprime } from './PpFerronor';

export default PpEprime;
